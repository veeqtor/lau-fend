import { SidebarOverlayDirective } from './sidebar-overlay.directive';

describe('SidebarOverlayDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarOverlayDirective();
    expect(directive).toBeTruthy();
  });
});
